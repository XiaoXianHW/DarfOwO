---
title: "Markdown 样式展示"
excerpt: "展示优化后的 Markdown 渲染效果，包括标题、代码块、图片预览等"
tags: ["示例", "样式", "文档"]
date: "2024-01-10"
author: "XiaoXian"
---

# 一级标题样式展示

这是一段普通的段落文本。这段文本用来展示默认的文本样式，包括**字体大小**、*行高*和颜色。

## 二级标题样式

### 三级标题样式

#### 四级标题样式

##### 五级标题样式

###### 六级标题样式

---

## 文本样式

这是一段包含各种**粗体文本**和*斜体文本*的段落。你还可以使用`内联代码`来高亮显示代码片段。

### 链接样式

访问 [React 官方文档](https://react.dev) 了解更多信息。

---

## 列表样式

### 无序列表

- 第一项内容
- 第二项内容
- 第三项内容
  - 嵌套项 1
  - 嵌套项 2

### 有序列表

1. 首先做这个
2. 然后做那个
3. 最后完成这个
   1. 子步骤 A
   2. 子步骤 B

### 任务列表

- [x] 完成项目初始化
- [x] 实现基础功能
- [ ] 添加代码高亮
- [ ] 编写文档
- [ ] 发布第一版

---

## 代码块样式

下面是一个 JavaScript 代码示例，悬停时会显示复制按钮：

```javascript
const greeting = (name) => {
  console.log(`Hello, ${name}!`)
  return `Welcome to DarfOwORD`
}

// 调用函数
greeting('World')
```

Python 代码示例：

```python
def calculate_sum(a, b):
    """计算两个数的和"""
    result = a + b
    return result

print(calculate_sum(5, 3))
```

---

## 引用块样式

> 💡 **提示**
>
> 这是一个提示信息，用于向读者提供有用的建议。

> ⚠️ **注意**
>
> 这是一个警告信息，需要特别注意的内容。

> ℹ️ **信息**
>
> 这是一条一般性的信息说明。

> 这是一个普通的引用块，没有特殊的类型标识。引用块使用左侧彩色线条来区分不同类型，配合淡色背景营造层次感。

---

## 图片展示

点击下面的图片可以放大预览查看：

![示例图片 - React Logo](https://via.placeholder.com/800x400/3b82f6/ffffff?text=Click+to+Preview)

*提示：点击图片可以放大查看，支持缩放功能*

你也可以插入多张图片：

![示例图片 2](https://static.axtn.net/img/Darf.jpg)

![示例图片 3](https://via.placeholder.com/700x350/ec4899/ffffff?text=Image+3)

---

## 表格样式

| 功能 | 状态 | 描述 |
|------|------|------|
| 代码高亮 | ✅ 完成 | 支持多种语言 |
| 图片预览 | ✅ 完成 | 点击放大 |
| 任务列表 | ✅ 完成 | 支持勾选框 |
| 主题切换 | ✅ 完成 | 亮色/暗色 |

---

## 混合内容示例

在开发过程中，我们经常需要使用 `npm install` 命令来安装依赖。例如：

```bash
# 安装项目依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build
```

安装完成后，你可以访问 [localhost:3000](http://localhost:3000) 查看效果。

> 💡 **提示**
> 
> 首次运行前请确保已安装 Node.js 18+ 版本。

---

## 总结

这个文档展示了优化后的 Markdown 渲染效果，包括：

1. **现代化的标题样式** - 带下划线的 H1/H2
2. **优雅的代码块** - 悬停显示复制按钮
3. **可交互的图片** - 点击放大预览
4. **美观的引用块** - 多种类型的提示框
5. **精致的文本样式** - 更好的阅读体验

