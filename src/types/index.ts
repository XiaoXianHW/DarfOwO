export type SideType = 'side1' | 'side2' | null;

export interface ClipPaths {
  clip1: string;
  clip2: string;
  divider: string;
}

export interface AvatarAnimation {
  top: string;
  left: string;
  x: string;
  y: string;
  scale: number;
  rotate: number;
}
